#pragma once
using namespace std;
#include <iostream>
#include <string>
#include <iomanip>
#include <vector>
#include <map>

//int check(string* s, char name, int size) {
//	for (size_t i = 0; i < size;++i ) {
//		if (s[i][0] == name) { return 1; }
//		else { return 0; };
//	}
//}
//int find() {}
//void AddChar(graph &histogram,char name ) {
//	if (check(histogram.bn, name, sizeof(histogram.bn) / (sizeof(char)))){
//
//	}
//

}